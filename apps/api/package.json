{
  "name": "@expandiq-agentkit/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@expandiq-agentkit/runtime-contracts": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.build.json"
  }
}
